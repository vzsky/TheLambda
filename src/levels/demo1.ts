import { LevelConfig } from "./_interfaces"

const information:string[] = [ "true", "false" ]
const solution:string[] = ["(λ(λ212))", "(λ(λ21(λ(λ1))))"]
const taskDescription:string = "you are given true, false function, construct and function. And function takes two argument and return boolean and and them"
const levelConfig:LevelConfig = {information, solution, taskDescription}

export default levelConfig