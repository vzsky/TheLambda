import { LevelConfig } from "./_interfaces"

const information:string[] = [ "iden" ]
const solution:string[] = ["(λ1(λ1))"]
const taskDescription:string = "you are given identity function, please construct a function that takes two arguments, and return the second one"
const levelConfig:LevelConfig = {information, solution, taskDescription}

export default levelConfig